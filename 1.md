You are a senior Python machine learning engineer.

Build a complete Python project that reproduces the algorithmic workflow of the paper:

"Portable Electrochemiluminescence Microarray Sensor for Machine Learning-Assisted Quantitative Analysis of Zearalenone"

The goal is to reproduce the computational workflow only, not the wet-lab experiment.

The paper workflow is:

1. Smartphone captures ECL luminescence images.
2. RGB features are extracted from valid luminescent regions.
3. KNN is used for concentration classification.
4. GPR is used for continuous ZEN concentration prediction.
5. 3-fold cross-validation is used.
6. Outputs include classification metrics, regression metrics, RGB plots, calibration curve, confusion matrix, predicted-vs-true plot, and Bland-Altman plot.

Do not invent missing experimental details. Make reasonable engineering choices only where needed, and expose them as configurable parameters.

Important coding style requirements:

* Write clean, human-like production code.
* Do not add code comments.
* Do not add long explanatory docstrings.
* Use clear function names and variable names instead of comments.
* Avoid over-engineered abstractions.
* Keep each file focused and readable.
* Use type hints.
* Handle missing files, invalid folders, invalid concentrations, and empty images gracefully.
* Print concise progress messages.
* Save all generated outputs into an outputs/ folder.

Create this project structure:

ecl_zen_ml/
README.md
requirements.txt
pyproject.toml
src/
**init**.py
config.py
dataset.py
features.py
models.py
metrics.py
plots.py
pipeline.py
main.py
scripts/
run_images.py
run_csv.py
outputs/
.gitkeep

The project must support two input modes.

Mode 1: image dataset

Expected folder format:

data/images/
0.0001/
sample_001.png
sample_002.png
0.0005/
sample_001.png
0.001/
0.005/
0.01/
0.05/
0.1/
0.5/
1/
10/
50/
100/

Each folder name is the ZEN concentration in ng/mL.

Mode 2: pre-extracted CSV

Expected CSV columns:

sample_id, concentration_ng_ml, R_mean, G_mean, B_mean, R_std, G_std, B_std, intensity_mean, intensity_std

Optional CSV columns:

spot_area, background_mean, signal_to_background_ratio, image_path

Core requirements:

1. Image feature extraction

Implement image feature extraction using OpenCV.

For every image:

* Load the image.
* Convert BGR to RGB correctly.
* Detect the ECL spot region.
* Support two ROI methods:

  * auto threshold ROI
  * center crop ROI fallback
* Extract these features:

  * R_mean
  * G_mean
  * B_mean
  * R_std
  * G_std
  * B_std
  * intensity_mean
  * intensity_std
  * spot_area
  * background_mean
  * signal_to_background_ratio
* Save all extracted features to outputs/extracted_features.csv.

The ROI parameters should be configurable in config.py:

* threshold_percentile
* min_spot_area
* center_crop_ratio
* use_auto_roi
* background_margin

2. Dataset handling

Implement utilities to:

* Load images from concentration-named folders.
* Parse folder names into float concentration values.
* Load pre-extracted CSV.
* Create classification labels from concentration values.
* Create continuous regression labels.
* Create log10 concentration values.
* Reject invalid concentration values.
* Drop rows with missing required features.
* Save a cleaned dataset to outputs/cleaned_features.csv.

Default feature columns:

R_mean, G_mean, B_mean, R_std, G_std, B_std, intensity_mean, intensity_std

Allow the feature columns to be changed in config.py.

3. KNN classification

Implement KNN concentration classification using scikit-learn.

Requirements:

* Use StandardScaler inside a Pipeline.
* Use KNeighborsClassifier.
* Search k values: 1, 3, 5, 7, 9.
* Search weights: uniform, distance.
* Use Euclidean distance.
* Use 3-fold StratifiedKFold when possible.
* If some classes have too few samples, fall back to KFold and print a warning.
* Select the best model by mean accuracy.
* Report:

  * accuracy
  * macro F1
  * weighted F1
  * per-class precision, recall, F1
  * confusion matrix
* Save metrics to outputs/knn_metrics.json.
* Save cross-validation predictions to outputs/knn_predictions.csv.
* Save confusion matrix plot to outputs/knn_confusion_matrix.png.

4. GPR regression

Implement Gaussian Process Regression for continuous concentration prediction.

Requirements:

* Predict log10(concentration_ng_ml) by default.
* Use StandardScaler inside a Pipeline.
* Use GaussianProcessRegressor.
* Try these kernels:

  * ConstantKernel * RBF
  * ConstantKernel * RBF + WhiteKernel
  * ConstantKernel * Matern + WhiteKernel
* Use 3-fold cross-validation.
* Select the best model by RMSE.
* Report:

  * MAE
  * RMSE
  * R2
  * median absolute error
* Save metrics to outputs/gpr_metrics.json.
* Save predictions to outputs/gpr_predictions.csv.
* Save plots:

  * outputs/gpr_predicted_vs_true_log.png
  * outputs/gpr_predicted_vs_true_concentration.png
  * outputs/bland_altman_plot.png.

5. Calibration curve

Implement a calibration analysis.

Use:

intensity_mean vs log10(concentration_ng_ml)

Fit a linear regression:

intensity_mean = slope * log10(concentration_ng_ml) + intercept

Report:

* slope
* intercept
* R2
* whether the relationship is signal-off or signal-on

The paper reports a signal-off relationship, meaning higher ZEN concentration leads to lower ECL signal. Do not force a negative slope. Fit the user's data and report the result.

Save:

* outputs/calibration_metrics.json
* outputs/calibration_curve.png

6. RGB analysis plots

Generate these plots:

* RGB channel mean vs concentration
* RGB channel mean vs log10 concentration
* RGB heatmap with samples as rows and R/G/B values as columns
* Separate R, G, B heatmaps if possible
* Example ECL image panel ordered by concentration if image input is used

Save:

* outputs/rgb_vs_concentration.png
* outputs/rgb_vs_log_concentration.png
* outputs/rgb_heatmap.png
* outputs/rgb_channel_heatmaps.png
* outputs/example_ecl_images.png

7. Figure 5 style summary

Create a Figure 5 style summary output.

It should contain:

* Panel A: representative ECL images ordered by concentration
* Panel B: RGB heatmaps
* Panel C: RGB values vs concentration
* Panel D: workflow diagram

Save it as:

outputs/figure5_style_summary.png

If image input is not available, skip Panel A and still generate the remaining panels.

8. CLI behavior

The project should run with:

python -m src.main --mode images --input data/images --output outputs

and:

python -m src.main --mode csv --input data/features.csv --output outputs

Optional arguments:

--feature-columns
--target-log
--random-state
--folds
--no-plots
--roi auto
--roi center

9. README.md

Write a useful README.

Include:

* Project purpose
* What part of the paper is reproduced
* What is not reproduced
* Required data formats
* Installation steps
* How to run image mode
* How to run CSV mode
* Output file descriptions
* Notes about limitations

The README should clearly state:

* This project reproduces the algorithmic workflow only.
* Exact paper results cannot be guaranteed without the original image dataset and hidden preprocessing details.
* The wet-lab ECL sensor construction is not reproduced.
* Users should tune ROI parameters and model parameters for their own dataset.

10. requirements.txt

Include:

numpy
pandas
scikit-learn
opencv-python
matplotlib
scipy
joblib

Avoid unnecessary packages.

11. pyproject.toml

Add a simple pyproject.toml with basic project metadata and Python version requirement.

12. Output standards

All metrics should be saved as JSON.

All predictions should be saved as CSV.

All plots should be saved as PNG at 300 dpi.

All paths should work on Windows, macOS, and Linux.

13. Final response

After generating the files, briefly tell me:

* What files were created
* How to install
* How to run with images
* How to run with CSV
* What outputs to check first

Do not include code comments anywhere in the generated source files.
